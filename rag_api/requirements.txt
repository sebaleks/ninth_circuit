fastapi==0.135.1
uvicorn[standard]==0.42.0
pydantic==2.12.5
faiss-cpu==1.14.2
pyarrow==23.0.1
pandas==2.3.3
numpy==2.4.3
openai==2.28.0
requests==2.32.5
python-dotenv==1.0.1
rank-bm25==0.2.2
qdrant-client==1.12.1
# Local in-process embedder (e5-small-v2) for the no-NIM retrieval path.
# CPU-only torch via the pytorch CPU index — the +cpu wheel avoids the multi-GB
# CUDA wheel PyPI would otherwise resolve.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
sentence-transformers==3.4.1
